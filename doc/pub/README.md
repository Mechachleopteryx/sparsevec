**NOTE**: The documents in this directory are compiled from Doconce
source in `../src`. *Never* edit the `.html` files here in `pub`, always
edit the Doconce source in `../src` and run `make.sh` to compile and
copy to `pub`.